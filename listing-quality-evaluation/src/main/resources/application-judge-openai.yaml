spring:
  ai:
    model:
      chat: openai
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        model: ${EVALUATION_JUDGE_MODEL:gpt-5.6-sol}
        max-completion-tokens: 1000
        temperature: 1
