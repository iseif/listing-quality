/**
 * The HTTP boundary of the enrichment API.
 *
 * <p>The package is null-marked so the nullability this code declares is checked against the
 * contracts it inherits: Spring's {@code ResponseEntityExceptionHandler} is null-marked too, and
 * a JSpecify {@code @Nullable} only carries meaning inside null-marked scope.
 */
@NullMarked
package dev.iseif.listingquality.enrichment.controller;

import org.jspecify.annotations.NullMarked;
